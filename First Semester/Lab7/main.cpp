#include <iostream.h>
#include <string.h>

int main()
{
    char input[50];

    printf("Input:\n");
    scanf("\s", input);

    /**
     * Make one line calculator
     * 
     * scanf()
     * sscanf()
     * sprintf()
     * strstr()
     * strcomp()
     * strcut()
     * strcopy()
     * 
     */

    return 0;
}

char** calcStack(char* input)
{
    char* st = 
    for (int i = 0; i < sizeof(input); i++)
    {
        
    }
    
}